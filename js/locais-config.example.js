/* Modelo do arquivo de configuração da chave do Google Maps.
   Copie este arquivo para js/locais-config.js (que é ignorado pelo
   Git) e coloque sua própria chave no lugar de SUA_CHAVE_AQUI.

   A chave precisa ter a "Maps JavaScript API" e a "Geocoding API"
   ativadas no Google Cloud Console, e deve ser restrita por
   HTTP referrer (domínio do site + localhost) antes de usar em
   produção. */
window.GOOGLE_MAPS_API_KEY = "SUA_CHAVE_AQUI";
